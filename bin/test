#!/bin/bash

set -eu
source $(dirname $0)/env.sh

function run_tests {
    ./tests/bin/unit_tests $@
    ./tests/bin/integration_tests $@
}

if [[ ${1:-} == "-v" ]]; then
    export GLOG_v=2
    export GLOG_logtostderr=true
    shift 1
fi

cd ${BUILDDIR}
time run_tests $@
